// Backward compatibility export
// This file maintains the old import path while the actual engine has moved to core/engine/
export { GameEngine, gameEngine } from './engine/gameEngine';
export type { ValidationResult } from './engine/gameEngine';
